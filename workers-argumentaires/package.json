{
	"name": "workers-argumentaires",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --config ../wrangler.toml",
		"dev": "wrangler dev --config ../wrangler.toml --env dev",
		"start": "wrangler dev --config ../wrangler.toml --env dev",
		"test": "vitest",
		"cf-typegen": "wrangler types --config ../wrangler.toml"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.38.0"
	}
}
